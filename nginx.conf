server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index demo.html;

    location = / {
        return 302 /examples/demo.html;
    }

    location / {
        try_files $uri =404;
    }
}
